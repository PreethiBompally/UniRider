<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UniRider.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize this theme to match the style of your MainActivity, if needed -->
    </style>

    <style name="Theme.UniRider.ActionBar" parent="Theme.Material3.DayNight">
        <!-- Customize your theme to include an ActionBar -->
        <!-- <item name="colorPrimary">@color/my_primary_color</item> -->
        <!-- <item name="colorPrimaryVariant">@color/my_toolbar_color</item>
        <item name="colorOnPrimary">@color/my_toolbar_color</item> -->
    </style>
</resources>